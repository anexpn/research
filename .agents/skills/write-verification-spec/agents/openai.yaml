interface:
  display_name: "Write Verification Spec"
  short_description: "Draft and review verification specs from behavior contracts"
  default_prompt: "Use $write-verification-spec to extract contracts, then draft or revise a Markdown verification spec with Subject, Observation, Pattern, and Statement sections using only Scenario, Property, and Progress patterns. Review it point by point for completeness and effectiveness before finalizing."
