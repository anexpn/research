# Ideas

This directory contains research ideas and small prototypes.

## Structure

- Each idea lives in a folder named `<NNN>-<slug>`.
- Example: `004-streaming-parser`.

## Workflow

- Keep each idea self-contained (code, notes, tests).
- Iterate quickly and keep experiments lightweight.
