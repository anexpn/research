# 004 Streaming Parser

Streaming parser experiments for incremental clause extraction.

## Includes

- Character-by-character parser prototypes.
- Decimal-safe delimiter handling.
- Optional wrapper skipping for `()` and `{}`.
- Property-based tests with `pytest` + `hypothesis`.

## Run

- `uv run pytest`
